# Dependencies
node_modules/

# Build output (generated via `npm run build:*`, contains a copy of local secrets)
dist/

# Local secrets & runtime state (regenerated by the setup wizard / EventSub / Riot polling)
.env
config.json
tokens.json
state.json
challenge.json

# Logs
*.log
npm-debug.log*

# Editor / tool local settings
.claude/settings.local.json
.vscode/
.DS_Store
Thumbs.db
